# 책방 운영 — 새 책 배포 프로세스

책방(오리지널 연재소설)의 **진실의 원본은 Supabase DB**(books / book_episodes)다.
웹은 DB를 ISR(5분 캐시)로 서버 렌더링하고, 앱(korean_typing)도 같은 DB를 읽는다
→ **퍼블리시 한 번으로 웹·앱 동시 송출, 웹 재배포 불필요.**

```
content/original-works/*.md   ← 원고 작성·교정 (git 백업)
        │
        └─ npm run books:publish → Supabase books/book_episodes upsert
                 │
                 ├─ 웹: DB에서 ISR 렌더링 (최대 5분 내 반영, SEO용 전문 HTML 유지)
                 └─ 앱: DB 직접 조회 (즉시)
```

- `npm run books:build`(정적 데이터 재생성)는 이제 **DB 장애 대비 폴백**용이다.
  DB를 못 읽으면 웹이 마지막 빌드 시점의 정적본으로 동작한다.

## 새 책 올리기 ① — 관리자 페이지 (기본)

**`/adminsangwon`** 에서 게재한다. 접근 조건 3가지가 모두 맞아야 한다:
1. 허용 IP (`ADMIN_ALLOWED_IPS`, 기본 193.186.4.174 — 그 외에는 404로 위장)
2. 카카오 로그인 이메일이 `ADMIN_EMAILS`에 포함
3. Vercel 환경변수에 `SUPABASE_SERVICE_ROLE_KEY` / `ADMIN_EMAILS` / `ADMIN_ALLOWED_IPS` 설정

절차: 새 책 게재 → 폼 입력(아이디·제목·작가 연결·표지 업로드) → 원고 붙여넣기
("## 1화. 제목" 구분, 화 분할 미리보기 자동) → **게재하기 = 웹 즉시 + 앱 다음 조회부터.**
작가는 "새 작가 등록"에서 필명·소개·SNS·블로그·프로필 사진(Storage 자동 업로드)으로 만든다.
검색엔진 색인 금지(noindex) + 허용 IP 외 404라 외부에서는 존재 자체가 보이지 않는다.

## 새 책 올리기 ② — 원고 파일 + 스크립트 (예비)

1. `content/original-works/책제목.md` 작성 (형식은 아래 참고)
2. 표지: `public/images/book-covers/{아이디}.jpg` (git push 필요) — 없으면 절차 생성 표지
3. `npm run books:publish -- --dry-run`으로 미리보기 → `npm run books:publish`
4. 원고 git 커밋 & push (백업 + 폴백 정적본 최신화는 `npm run books:build` 후 커밋)

⚠️ 같은 책을 두 경로로 번갈아 수정하면 나중에 실행한 쪽이 덮어쓴다. 한 책은 한 경로로만.

## 화(에피소드) 추가·본문 수정

같은 md 파일을 고치고 위 3→6 반복. ID(`아이디`, 화 번호)가 키라서
upsert로 덮어써지고, 필사 기록(`pilsa_records.source_id`)도 그대로 이어진다.
**아이디와 화 번호는 게재 후 절대 바꾸지 말 것** (기록 연결이 끊긴다).

## 연재 중 vs 완결

`구성: 전 N화`의 N이 **실제 올린 화 수보다 크면 "연재 중"**으로 표시된다.
예: `구성: 전 12화`로 선언하고 5화까지만 쓰면 → 웹·앱 모두 "연재 중 · 5화까지 공개".
화를 다 채우면 자동으로 "완결"이 된다.

## 원고 형식 (md 메타 블록)

```markdown
# 책 제목

- **아이디**: batchim            ← 영문 소문자, 게재 후 변경 금지
- **표지**: amber / dots         ← 팔레트/패턴 (rose·indigo·emerald·amber·ink·plum / grid·wave·dots·diag)
- **형식**: 오리지널 동화 (한글타자왕 오리지널 2호)   ← "N호"가 진열 순서, "동화" 포함 시 동화/초급 분류
- **구성**: 전 10화, ...          ← "전 N화"가 예정 화수 (연재 중 판정 기준)
- **작가**: 한이음               ← 없거나 "미정"이면 "작가 표기" → 기본값 순
- **로그라인**: 한두 문장 소개    ← 카드·OG 설명에 쓰임
- **소개**: 상세 소개 (선택, 없으면 로그라인 사용)
- **작가 SNS**: https://instagram.com/...      ← 이하 전부 선택 (투고 보상 링크 게재)
- **작가 블로그**: https://blog.naver.com/...
- **작가 프로필**: /images/authors/haneum.jpg  ← public/images/authors/ 에 넣고 사이트 상대경로로
- **작가 소개**: 한 줄 소개

---

## 1화. 화 제목

본문... (빈 줄 2개 = 문단 구분, 모바일 필사 단위)
```

## 작가 시스템 (투고 보상: SNS·블로그 링크 게재)

작가는 `authors` 테이블로 관리한다 (한 작가 → 여러 작품). 원고 메타에:

```markdown
- **작가 아이디**: haneum          ← authors 슬러그. 있으면 /authors/haneum 작가 페이지 생성
- **작가 SNS**: https://...        ← 이하 선택. publish 시 authors 테이블에 병합됨
- **작가 블로그**: https://...
- **작가 프로필**: /images/authors/haneum.jpg  (또는 Storage URL)
- **작가 소개**: 한 줄 소개
```

- **작가 페이지** `/authors/{아이디}` — 프로필 + 그 작가의 작품 목록이 **자동으로** 쌓인다
- **시리즈 페이지** 작가 카드에서 "작품 더 보기 →"로 연결
- 같은 아이디의 원고가 여러 개면 프로필 필드는 병합된다 (한 원고에만 써도 됨)
- 기존 9권: 한이음(`haneum`, 일곱 번째 공책) / 한글타자왕 오리지널(`tajawang`, 나머지 8권)

## 이미지 저장소 (Supabase Storage)

`book-assets` 공개 버킷 (생성 완료). admin(3단계)이 여기로 업로드한다:
- `covers/{책아이디}.jpg` — 표지
- `authors/{작가아이디}.jpg` — 작가 프로필
- 공개 URL: `{SUPABASE_URL}/storage/v1/object/public/book-assets/...`
- 기존 9권 표지는 `public/images/book-covers/`(저장소) 그대로 둔다 (옮길 필요 없음)

## 인프라 메모

- **Supabase 테이블** (1회 설치: `supabase/books.sql` + `supabase/authors.sql`을 대시보드 SQL Editor에서 실행)
  - `books` — 시리즈 메타 (공개 읽기 전용, `author_id` → authors)
  - `authors` — 작가 프로필 (공개 읽기 전용, id = URL 슬러그)
  - `book_episodes` — 화 본문 (공개 읽기 전용, id = `{아이디}_ep{N}` = `pilsa_records.source_id`)
  - 쓰기는 service_role만 → `.env.local`의 `SUPABASE_SERVICE_ROLE_KEY` 필요 (stats와 같은 키)
- 소셜(조회·좋아요·댓글)은 `supabase/book_social.sql`, 서재 동기화는 `supabase/pilsa_records.sql` (적용 완료)
- publish는 원고에서 사라진 화만 정리하고, **책 자체는 자동 삭제하지 않는다** (경고만)
