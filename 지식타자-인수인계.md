# 지식 타자 (구 "지식 여정") — 인수인계 문서

> 작성일 2026-07-19. 이 문서만 읽으면 이전 대화 없이 이어서 작업할 수 있도록 정리함.

## 1. 컨셉과 확정된 방향

**컨셉 (사용자 확정 문구): "지식을 쌓으며 타자 실력을 높인다"**

- 순서가 있는 지식(조선 왕조, 세계 수도, 주기율표 등)을 한 항목씩 타자로 정복하며 암기하는 학습 모드
- 항목 이름은 **초성 힌트**만 보고 떠올려 입력(인출 연습) → 맞히면 **핵심 지식 한 줄**이 공개되고 그것까지 타이핑해야 다음으로 진행 (암기 + 타자량 둘 다 확보)

**사용자 피드백으로 확정된 결정 (반드시 지킬 것):**

1. ~~"지식 여정"~~ → **"지식 타자"로 리네이밍** (아직 미반영, 남은 작업 #1)
2. **지하철 노선도 UI 제거** — 처음엔 바이럴 릴스(2호선 타자 시각화)에서 착안해 노선도 UI로 만들었으나, 사용자가 "남의 콘텐츠 베끼는 건 싫다"고 명확히 거부. 2호선 코스도 로드맵에서 이미 제거함. 노선/역/열차 메타포 자체를 빼고 **코스별 컨셉에 맞는 오리지널 UI**로 교체해야 함 (남은 작업 #2). 메모리 `journey-no-copycat.md` 참고
3. 다음 코스: **세계 수도, 주기율표** (한국사 연표·삼국 왕 계보도 사용자가 좋다고 함 — 그다음 순위)
4. **디자인 토큰 필수** — design.md의 시스템(surface 계층, No-Line Rule=1px 보더 금지·토널 레이어링, primary-gradient CTA, glass-card, editorial-heading). 이미 전면 적용됨
5. **노트북 한 화면 플레이** — 스크롤 없이 좌측 시각화 + 우측 계기판·프롬프트·입력이 다 보이는 2컬럼 레이아웃. 이미 적용됨(플레이 영역 높이 ~465px)

## 2. 구현 완료된 것 (동작 검증됨)

### 파일 구조

| 파일 | 역할 |
|---|---|
| `lib/journey-data.ts` | 데이터 모델 + 조선 왕조 27대 콘텐츠. `JourneyCourse { id, title, subtitle, description, category, emoji, keywords(SEO), groups(니모닉 청크), lines[{color, stations[]}] }`, `JourneyStation { id, name(타이핑1), fact(타이핑2), reading?, year?, detail?, group? }`. **코스 추가 = 이 파일에 객체 추가가 전부** (라우트·사이트맵·허브 자동 반영) |
| `lib/journey-progress.ts` | localStorage 진행 저장 (`tajawang_journey_v1`). 스냅샷(이어하기)·완주 누적·허브 요약. `pilsa-library.ts` 패턴 복제, 서버 동기화는 나중에 이 API 뒤에 붙이는 전제 |
| `components/journey/JourneyPlay.tsx` | 핵심 엔진. 상태머신 `ready/playing/finished` × `traveling(이름 입력)/arrived(지식 문장 입력)` × `stationIndex`. `TypingUtils`(lib/typing-speed.ts)로 자소 타수/정확도/등급. **IME 오타 판정**: 마지막 글자를 뺀 입력이 접두어가 아닐 때만 오타(`checkWrong`) — 한글 조합 중 오탐 방지, 절대 단순 startsWith로 되돌리지 말 것. 초성 힌트(`chosungOf`) + 힌트 버튼 + 이름 전체 보기 토글. 일시정지 시간 보정(TypingRaceGame 패턴). 모바일은 `useMobileGamePlay`+`MobileGameShell`(다크 풀스크린 셸, 기존 게임 공통) |
| `components/journey/JourneyMap.tsx` | ⚠️ **교체 대상.** 현재 지하철 노선도 SVG(지그재그 boustrophedon + strip 모바일 변형). 남은 작업 #2에서 코스별 컨셉 UI로 대체 |
| `components/journey/JourneyComplete.tsx` | 완주 모달(글래스, 토큰). 타수/정확도/시간/등급 + 클립보드 공유 + 재도전 |
| `components/journey/JourneyHubProgress.tsx` | 허브 카드 진행 배지(클라이언트, localStorage hydrate) |
| `app/journey/page.tsx` | 허브. 코스 카드 그리드 + SEO 섹션. ⚠️ 카피에 "지하철 노선" 표현 다수 — 리네이밍 때 함께 수정 |
| `app/journey/[courseId]/page.tsx` | 코스 페이지. `generateStaticParams`+`dynamicParams=false`, per-course 메타데이터, 플레이 본체 + **SEO 보너스: 전체 항목 표**(복습 겸 검색 랜딩) + 학습 팁 |

### 기존 파일 통합 (완료)

- `components/layout/Header.tsx` — 데스크톱/모바일 내비에 "지식 여정" 추가(TramFront 아이콘 — 리네이밍 때 아이콘도 교체 권장), 맞춤법 퀴즈는 `hidden xl:flex`로 강등. 1024px 한 줄 유지 확인됨
- `app/HomeClient.tsx` — FeatureCard 6개 그리드(step 04)
- `app/sitemap.ts` — `/journey` + 코스별 URL
- `lib/analytics.ts` — `UsageEventName`에 `journey_start | journey_complete | journey_hint` 추가, 플레이에서 track 호출 중
- `.claude/launch.json` — **dev 서버는 `dev-node22` 구성 사용** (기본 node 18로는 Next 16이 안 뜸; nvm v22.21.0 경로 하드코딩)

### 검증된 것

- 전체 플로우 실플레이 검증: 시작 → 초성 보고 이름 입력 → 도착 카드(연도+지식+부가설명) → 문장 입력 → 다음 역 + 스냅샷 저장 → 27역 완주 → 완주 모달 → 완주 기록 누적 → 허브 배지("완주 1회 · 최고 180타")
- 새로고침 후 "N번째 역부터 이어가기" 동작, 오타 시 rose 색 피드백 + mistakes 카운트(1회/연속)
- `npx tsc --noEmit` / `npm run build` 통과 (`/journey` 정적, `/journey/joseon-kings` SSG)
- ⚠️ 미검증: 모바일 풀스크린 셸(`pointer: coarse` 기기 전용이라 데스크톱 에뮬레이션으로 확인 불가) — **실기기 확인 필요**

## 3. 남은 작업 (우선순위순)

### #1 리네이밍: "지식 여정" → "지식 타자"

- 노출 문구 전부: Header 라벨, HomeClient 카드, 허브/코스 페이지 제목·메타데이터·OG·sr-only h1, 완주 공유 문구("한글타자왕 지식 여정 …")
- 컨셉 카피: "지식을 쌓으며 타자 실력을 높인다"를 허브 헤더/메타 description에 반영. "노선·역·열차·개통" 등 지하철 어휘 전면 제거
- **라우트 `/journey`는 유지 권장** (이미 빌드·사이트맵 반영, 한글 slug 불가). 내부 코드명(journey-*)도 유지하고 표기만 변경하면 변경 범위 최소
- Header 아이콘 TramFront → 지식 느낌(예: `BrainCircuit`, `GraduationCap`, `Sparkles`)으로 교체

### #2 노선도 제거 → 코스별 컨셉 UI

`JourneyMap.tsx`(full/strip 두 variant)를 대체. 엔진(JourneyPlay)의 상태머신·판정·저장은 그대로 두고 **시각화 컴포넌트만 갈아끼우면 됨** (props: stations, currentIndex, phase, finished, showAllNames). 코스 카테고리별 UI 아이디어:

- **주기율표 (과학)**: 실제 주기율표 그리드에서 원소 칸이 하나씩 채워지는 UI — 컨셉이 자명하고 오리지널. `reading` 필드에 원소 기호(H, He…) 넣어 칸에 표기, name은 "수소" 등 한글명. 1~20번 원소 정도로 시작 권장
- **세계 수도 (지리)**: 국기 이모지 카드 그리드(나라 카드 뒤집기/채우기) 또는 대륙별 섹션 진행. `name`=수도, 프롬프트에 나라명+국기 표시(=문제), `fact`는 나라 관련 한 줄 상식. 데이터 구조상 "문제 표시용" 필드가 필요하면 `reading`을 나라명으로 활용 가능
- **조선 왕조 (역사)**: 연대기 타임라인/족보 두루마리 등으로 교체 (노선 메타포만 제거하면 됨)
- 공통: 진행률·완료 체크·현재 항목 하이라이트는 유지, 디자인 토큰 준수, 데스크톱 좌측 카드에 들어가는 사이즈(높이 ~490px 이내), 모바일 셸용 컴팩트 변형 1개
- 관련 잔재 정리: `JourneyLine.loop` 필드(순환선용이었음 — 미사용이면 제거 가능), groups 라벨 렌더링은 코스별 UI에 맞게 재해석(주기율표는 주기/족, 세계 수도는 대륙)

### #3 세계 수도·주기율표 코스 데이터

- `lib/journey-data.ts`에 `JOURNEY_COURSES` 객체 추가 + 콘텐츠 작성 (fact 문장은 `TypingUtils.normalize` 친화적으로 — 특수문자·줄표 금지, 10~25자)
- 허브 "새 노선 공사 중" 카드 문구 갱신, sitemap은 자동 반영됨

### 이후 로드맵 (사용자 승인됨)

한국사 연표, 삼국 왕 계보 → 랭킹(`game_scores` 재사용, `SupabaseService.saveGameScore('journey-'+courseId, …)`) → 서버 진행 동기화(pilsa_records 패턴)

## 4. 주의사항

- **오리지널만**: 남의 바이럴 콘텐츠 재현 금지 (메모리 `journey-no-copycat.md`)
- Supabase는 Flutter 앱(korean_typing)과 공유 — 공유 테이블 스키마 변경 주의. 현재 지식 타자는 DB 미사용(전부 정적 TS + localStorage)이라 해당 없음
- dev 중 광고 iframe이 계속 리사이즈되어 스크린샷 검증이 불안정 — DOM/JS 기반 검증 병행 권장
- 사용자 dev 서버가 3000 포트에 떠 있는 경우가 많음 — 죽이지 말고 그대로 사용 (HMR 반영됨)
